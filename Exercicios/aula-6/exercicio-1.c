#include <stdio.h>

int i, valores;
float a,b,c;

printf("Digite os valores para i, a, b e c respectivamente");
scanf("%d%f%f%f",&i,&a,&b,&c);
switch(i) {
    case 1: if(( a > b) && (b > c) && (c>a)) {
        printf(" %f, %f, %f", a,b, c);
    } else if ((a<b) && (b>c)) {
        printf(" %f, %f, %f", b,a, b);
    } else if (

    )
}